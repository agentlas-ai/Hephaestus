description = "Run Hephaestus to create/package Agentlas agents or open the ontology GUI."
prompt = """
# /hephaestus

Raw arguments:
{{args}}

Use Hephaestus, the Agentlas Core Engine Meta-Agent.

If this workspace contains `AGENTS.md`, `.agentlas/mode-map.json`, and
`.agentlas/global-commands.json`, read those files first and follow the local
package contract.

If this workspace does not contain the Hephaestus package files yet, explain
that the user should run the one-touch installer from an OS terminal:

`curl -fsSL https://raw.githubusercontent.com/agentlas-ai/Hephaestus/v0.6.1/scripts/install-all-runtimes.sh | bash`

For creation or packaging work, classify the request as single-agent-builder,
multi-agent-team-builder, or agentlas-packager. Generate or repair the Agentlas
package, verify it, and include the `global_commands` section from
`.agentlas/global-commands.json` in the final response.

If the first argument is `ontology`, open the project ontology GUI by running
`./bin/hephaestus ontology --gui .` when the local shell command is available,
then report `gui_url`, `db_path`, `inbox_path`, and verification status.
"""
